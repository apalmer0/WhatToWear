<p id="notice"><%= notice %></p>

<h1>Outfits</h1>

<table class="table table-header-rotated">
  <thead>
    <tr>
      <th></th>
      <%- @categories.each do |category| %>
        <th class="rotate">
          <div><span><%= category.name %></span></div>
        </th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @outfits.each do |outfit| %>
      <tr>
        <th class="row-header"><%= outfit.worn_on.strftime("%b %e, %Y") %></th>
        <%- @categories.each do |category| %>
          <%- if outfit.clothing_categories.include?(category) %>
            <td>X</td>
          <%- else %>
            <td></td>
          <% end %>
        <% end %>
        <td><%= link_to 'Show', outfit %></td>
        <td><%= link_to 'Edit', edit_outfit_path(outfit) %></td>
        <td><%= link_to 'Destroy', outfit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Outfit', new_outfit_path %>
